import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    background: {
      default: '#C4C1C1',
    },
    primary: {
      main: '#003366',
    },
    secondary: {
      main: '#03A9F4',
    },
    success: {
      main: '#4CAF50',
    },
    camera: {
      main: "#ffffff"
    },
    text: {
      primary: '#757575',
      secondary: '#424242',
    },
  },
});

export default theme;